import React, { Fragment } from 'react'

const ReactChildrenB = (props) => {

    return (
        <Fragment>
            {
                props.children
            }
        </Fragment>
    )
}

export default ReactChildrenB